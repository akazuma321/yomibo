# ===== Database (Production: Postgres recommended) =====
# Neon / Supabase / RDS などの Postgres 接続文字列を入れてください。
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# Prismaのマイグレーション用（NeonのDirect / Non-pooled 推奨）
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# ===== Auth.js / NextAuth =====
# 本番では必須（ランダム文字列）
AUTH_SECRET="replace-with-long-random-string"

# 本番URL（どちらか片方でも動きますが、両方入れるのが安全です）
AUTH_URL="http://localhost:3002"
NEXTAUTH_URL="http://localhost:3002"

# Google OAuth（任意）
AUTH_GOOGLE_ID="xxxxxxxxxxxx.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="xxxxxxxxxxxxxxxxxxxx"

# ===== Invite code (optional) =====
# 招待コードを必須にする場合:
INVITE_REQUIRED="1"
# 単一コードなら INVITE_CODE、複数なら INVITE_CODES（カンマ/改行区切り）
INVITE_CODE="YOMIBO-INVITE-001"
INVITE_CODES="YOMIBO-INVITE-001,YOMIBO-INVITE-002"
# Cookieの有効期限（秒）。招待コード入力後〜初回登録/ログインまで持てば良いので短めでOK
INVITE_COOKIE_MAX_AGE_SEC="86400"
# 任意: cookie署名用の別secret（未設定ならAUTH_SECRETを流用）
INVITE_COOKIE_SECRET=""

# ===== Optional: Semantic search =====
OPENAI_API_KEY=""

